from django.db import models

# We will list our database models related to authentication and authorization here.
# As of now it's empty because the User model and it's properties are present in 'user' app.
